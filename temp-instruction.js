import { PublicKey } from '@solana/web3.js'; const buf = Buffer.from([98, 222, 123, 1, 143, 111, 134, 160]); console.log('Snake Case Hash (initialize_project):', buf.toString('hex')); const buf2 = Buffer.from([175, 175, 109, 31, 13, 152, 155, 237]); console.log('Camel Case Hash (initializeProject):', buf2.toString('hex')); console.log('Now hash with code:'); const snakeCaseHash = Buffer.from(Array.from(new Uint8Array(await crypto.subtle.digest('SHA-256', new TextEncoder().encode('initialize_project'))))).slice(0, 8); console.log('Snake Case Computed:', snakeCaseHash.toString('hex')); const camelCaseHash = Buffer.from(Array.from(new Uint8Array(await crypto.subtle.digest('SHA-256', new TextEncoder().encode('initializeProject'))))).slice(0, 8); console.log('Camel Case Computed:', camelCaseHash.toString('hex'));
